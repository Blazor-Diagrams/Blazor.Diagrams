@inherits DiagramCanvasComponent

<div class="diagram-canvas"
     tabindex="-1"
     @ref="elementReference"
     @onmousedown="OnMouseDown"
     @onmousemove="OnMouseMove"
     @onmousemove:preventDefault
     @onmouseup="OnMouseUp"
     @onkeydown="OnKeyDown">

    @* Links *@
    <svg class="diagram-links"
         style="transform: translate(@(DiagramManager.Pan.X)px, @(DiagramManager.Pan.Y)px);">
        @foreach (var link in DiagramManager.AllLinks)
        {
            <CurvedLink @key="link.Id" Link="link" />
        }
    </svg>

    @* Nodes *@
    <div class="diagram-nodes"
         style="transform: translate(@(DiagramManager.Pan.X)px, @(DiagramManager.Pan.Y)px);">
        @foreach (var node in DiagramManager.Nodes)
        {
            <NodeRenderer @key="node.Id" Node="node" />
        }
    </div>
</div>