@page "/demos/snaptogrid"
@inherits SnapToGridComponent
@layout DemoLayout
@inject LayoutData LayoutData

@code {
    protected override void OnInitialized()
    {
        base.OnInitialized();

        LayoutData.Title = "Snap to Grid";
        LayoutData.Info = "This diagram has a grid size of 50, moving nodes will make them automatically snap.";
        LayoutData.DataChanged();
    }
}

<CascadingValue Name="DiagramManager" Value="diagramManager">
    <DiagramCanvas>
        <Widgets>
            <Navigator Width="300" Height="200" DefaultStyle="true"></Navigator>
        </Widgets>
    </DiagramCanvas>
</CascadingValue>
